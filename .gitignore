# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.eggs/

# Environment
.env
.env.local
.env.production
.dev.vars

# Wrangler
.wrangler/
workers/.wrangler/

# Secrets — NEVER commit these
*.pem
*.key
*.p12
razorpay_secret.txt
api_keys.txt

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
logs/
npm-debug.log*

# Test artifacts
coverage/
.coverage
htmlcov/
report_output.json

# ── Deployment scripts & emergency fix logs (never commit) ────────────────────
*.bat
*.ps1
fix_deploy*.txt
*_result.txt

# ── Staged commit message drafts (already committed — leftover artifacts) ─────
commit_p*.txt

# ── Audit & generated output files ───────────────────────────────────────────
CYBERDUDEBIVASH_KV_AUDIT_REPORT.docx
tools_output/
workspace/

# ── Large binary artifacts / accidental repo snapshots ───────────────────────
# Source archives must never be committed (they bloat every clone and drift
# from the live tree). Build/release artifacts belong in CI artifact storage,
# not git. See ARTIFACT_RETENTION_POLICY.md.
*.zip

# ── Temporary files ───────────────────────────────────────────────────────────
*.tmp
*.bak
*.orig
.wrangler/state/

# ── Local runtime artifacts (SQLite dev DB, agent memory) ─────────────────────
# NB: anchored to repo root so the tracked ai-core/memory package is unaffected.
*.db
/cdb_ai.db
/memory/
/logs/

# CVE SEO generator data exports (transient)
tools/_cve_raw.json
tools/_cve_export.json
tools/_cve_err.txt

.dev.vars*
!.dev.vars.example
!.env.example

# Stray root-level wrangler config: wrangler 4.x auto-scaffolds a root
# wrangler.jsonc (assets-only, no bindings) whenever `wrangler` is run from the
# repo root. The REAL Workers config is workers/wrangler.toml — always deploy
# with: `npx wrangler deploy --config workers/wrangler.toml` (CI runs from
# workers/). Ignore the root scaffold so it can never be committed or reach CI.
/wrangler.jsonc
/wrangler.json
